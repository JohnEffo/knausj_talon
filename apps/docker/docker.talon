tag: terminal
and tag: user.docker
-
docker image:  
  "docker images"
  key(enter)

docker processes all:
   "docker ps -a"
   key(enter)

docker processes:
  "docker ps"
  key(enter)

docker compose:"docker-compose "
